import 'package:flutter/material.dart';

class navbarjamaah extends StatelessWidget {
  const navbarjamaah({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}